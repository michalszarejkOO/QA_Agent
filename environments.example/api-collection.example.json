{
  "product": "acme-api",
  "environment": "demo",
  "ticketPrefixes": ["ACME"],
  "bruno": {
    "repo": "~/Projects/acme-api",
    "collectionPath": "bruno",
    "defaultEnvironment": "demo",
    "knownCaveats": [
      "Name any stateful/order-dependent case here, e.g.: 'auth/verify-otp/01 depends on auth/request-otp/01 having run first for the same identifier.'"
    ],
    "confluence": {
      "cloudId": "your-site.atlassian.net",
      "spaceKey": "SPACE",
      "parentId": "0000000000",
      "pageTitle": "Acme API — Bruno Collection",
      "attachmentName": "acme-api-bruno-collection.zip",
      "notes": "Omit this whole confluence block if the product has nowhere to publish results — testing-bruno-api skips that step when it's absent."
    }
  },
  "notes": "This block can live in the same file as the app-with-ui shape if one product has both a UI and a Bruno API collection."
}
