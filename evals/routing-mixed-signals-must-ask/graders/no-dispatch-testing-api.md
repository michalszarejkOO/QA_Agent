---
type: tool_used
weight: 1
tool: Skill
input_match: '"skill"\s*:\s*"(?:[\w-]+:)?testing-api"'
min: 0
max: 0
---

Both backend- and frontend-shaped paths are touched and no label settles it —
the response must not silently pick backend and dispatch to `testing-api`.
